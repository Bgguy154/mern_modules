import { useState } from 'react'
import './App.css'

function App() {
  const [count, setCount] = useState(0)

  return (
    <>
    {/* The
useEffect Hook in React allows you to perform side effects
 in functional components, such as data fetching,
  subscriptions, or manual DOM manipulation. It is a way
   to synchronize your component with external systems and
    serves the combined purpose of lifecycle methods
     (like componentDidMount and componentDidUpdate)
      found in class components */}
    </>
  )
}

export default App
